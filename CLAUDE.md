# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Purpose

Exercise scripts from *Python Crash Course* (book PDF included). Each `.py` file is a standalone script — no packages, venv, or dependencies required.

## Running Scripts

```bash
python cars.py
python motorcycle.py
python name.py
```

Use mise-managed Python 3.14.4 (see `~/CLAUDE.md` for runtime details). No venv needed — these scripts have no third-party imports.
